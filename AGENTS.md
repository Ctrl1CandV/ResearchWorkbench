# 项目协作约定

这是独立新项目，不是原站原地改版。先读 docs/START-HERE.md、docs/SPEC.md 和当前 PLAN，再做分派与修改。

## 长期约束

- 首版内容少、闭环真实；不为长期扩展提前建设大平台。
- 初期主动给出有排序、有依据的探索路径，说明必读、必学与掌握标准；不要求新生先知道方向。
- 不承诺研究创新或发表，不把 CCF A 期刊与会议混同。
- private/legacy-export 是历史资料，不是当前需求；区分用户事实、AI 建议、来源覆盖和本人掌握状态。
- grad-companion 的阅读、每日发现等既有技能应优先复用；技能不是现成后端，接入必须实际核查。不改插件安装文件，不静默初始化或改写个人研究记录。
- private/、.grad/、密钥与未公开研究材料不得提交或对外上传。示例数据需明确标记，不得冒充真实阅读进度。

## 角色

主会话 gpt6astra 负责策划、设计、调度与最终验收。
research-workbench-scout 使用用户配置的 qwen3.8-max；builder 使用 glm-5.3-flash；reviewer 使用 gpt-6-astra。运行时以宿主配置和元数据为准。
使用命名智能体前验证可发现性。若 Agent 返回 type not found，报告会话发现限制，不反复猜名称，不用默认代理冒充指定模型。需要用户在本目录新开会话加载。

仅一个实现者修改同一文件集合。任务包必须明确工作区、文档版本、读写范围、验收标准与交付格式。审查者不直接修复代码。首版范围及设计未经审查不得下发产品实现。

不提交、不推送、不公开部署，除非用户明确授权。删除/覆盖已有资料必须先检查且取得所需授权；新仓库不能成为清理旧项目的理由。
