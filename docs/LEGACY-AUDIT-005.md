# LEGACY-AUDIT-005：旧版价值点审计与退役条件（2026-09-15）

> **执行结果（追加，2026-09-15）**：条件 A–D 已全部满足并执行退役——A/B/C 分别由 PLAN-005 阶段 1/2/3 满足；D 经用户确认（"没有笔记或已导出，可以删"）。§三 删除范围的 6 项已全部完成：四文件删除、server 白名单清理、index.html 入口移除、styles.css 旧版 321 行段裁掉、相关测试改写。**修正（2026-09-16）**：旧版四文件在首次提交之前即已删除，从未进入 git 历史，因此不能靠 git 恢复；可恢复的是 v2 浏览器数据（仍在用户浏览器 localStorage 中，失去查看入口）与本地 `private/legacy-export/` 历史导出。实施证据见 [ACCEPTANCE-006](ACCEPTANCE-006.md)。

回答一个问题：**旧版（legacy.html + app.js + domain.js + content.js + styles.css 前 321 行）的所有有价值点，是否已被新版覆盖？** 结论先行：**目前还没有，差两点加一个决策**。逐条审计如下。

## 一、旧版价值点清单与覆盖状态

| # | 旧版价值点 | 出处 | 新版状态 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | 三方向导航内容（agent/rag/peft：问题定义、methodCompare、风险、首读建议） | content.js directions | 新版三方向是 2026-09-15 用户批准的**不同结论**（代码修复验证/动态 RAG/图有害融合），旧方向已被取代而非覆盖 | **已被取代**，无迁移价值；旧方向文本留在 git 历史即可 |
| 2 | 六篇公开论文内容（Toolformer/ToolLLM/Gao RAG 综述/Lewis RAG/LoRA/Lialin PEFT 综述，含 mustRead/selfCheck/questions） | content.js papers | 已作为"按需支线"接入新版，保持原摘要级覆盖，不冒充精读 | **已覆盖**（PLAN-004 阶段 4 完成） |
| 3 | CCF A 目录事实块（期刊/会议分列、cautions、第七版核查日期与来源 URL） | content.js ccf | 新版**无对应内容**；P1.7（投稿信息）本轮决定暂缓 | **未覆盖** → 决策点 D3 |
| 4 | 画像问卷（6 字段）+ 规则打分 recommendDirections | content.js profile + app.js | 方向已由用户批准固定，"打分推荐方向"失去存在意义；且画像属本人记录，本就不该进公开产物 | **可放弃**（能力过时，非数据丢失） |
| 5 | 论文已读状态 + 个人备注（v2 localStorage，Web Locks 串行写、原文/草稿分离、Markdown 导出） | app.js + domain.js | 新版刻意不读写任何本地记录；PLAN-005 阶段 1 将建独立 v3 键补齐 | **未覆盖** → 依赖 PLAN-005 阶段 1 |
| 6 | "每日发现"视图（Crossref 三主题、点击触发、登记≠发表提示、错误态） | app.js renderDiscoveryView | 新版 server.mjs 有 /api/discover 接口但**前端没接**；PLAN-005 阶段 2 补齐 | **未覆盖** → 依赖 PLAN-005 阶段 2 |
| 7 | hostHandoff 手动交接模板（grad-radar 边界文案：不含个人记录、不代标已读、复制≠执行） | content.js hostHandoff | 新版无；其边界设计思想已被项目规则吸收，模板本身可被 v3 的 Markdown 导出替代（导出即可粘贴给任何模型） | **弱价值，可替代**，不单独迁移 |

## 二、结论与退役条件

旧版**现在不能删**。满足以下全部条件后可以删：

- **条件 A（功能）**：PLAN-005 阶段 1 完成——v3 本地键提供 已读状态/一条问题/笔记/Markdown 导出，且实测与 v2 互不读写。
- **条件 B（功能）**：PLAN-005 阶段 2 完成——每日精选区接通 /api/discover，三主题可用，提示语保留"近期登记≠近期发表""失败不冒充"。
- **条件 C（决策 D3 已执行）**：CCF 目录块有明确去向——要么按 PLAN-005 决策点 D3 把"期刊/会议分列 + 目录核查日期"一句话事实并入方向页，要么用户明确放弃。
- **条件 D（数据）**：用户确认已对 v2 旧笔记做过一次性导出（旧页面删除后浏览器 localStorage 里的 v2 数据不会消失，但失去查看入口；导出兜底）。

## 三、退役时的删除范围（满足条件后，一次性做）

1. `public/legacy.html`、`public/app.js`、`public/domain.js`、`public/content.js`；
2. `server.mjs` 资源白名单中对应条目；
3. `public/index.html` 侧栏"旧版笔记入口"链接与相关文案；
4. `public/styles.css` 前 321 行旧版样式段（届时新版样式自第 1 行起，需重新核对新版样式不依赖旧段的任何变量/规则）；
5. 相关测试（引用旧文件的用例）同步移除或改写；
6. git 历史完整保留，任何误删可恢复——**这正是把首次提交放在内容定稿之后的另一重理由**。

## 四、明确不做

- 不迁移 v2 数据到 v3（两套记录并存，v3 从零开始；用户如要旧笔记内容，用条件 D 的导出手工带过去）。
- 不保留"画像打分"的任何变体进新版（SPEC 已定：不继续扩张画像打分）。
