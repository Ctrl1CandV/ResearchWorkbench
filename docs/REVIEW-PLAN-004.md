# PLAN-004 实施独立验收

## 裁决
主体功能已落地，可试用；暂不签署“全部完成”。有局部P2待修与最终视觉/完整交互证据缺口，不需要重做架构。本次只审不修产品代码。

## 实际验证
- 主协调运行npm test：190项总数，189通过、0失败、1跳过。跳过为Windows符号链接权限。ACCEPTANCE-005:33记为190通过/0跳过，与本次实测不符。
- 浏览器实际加载新首页，四区用途、预览、入口、首次使用说明均可见。
- 真实locator点击首页“进入技术学习”，待hash渲染后显示六主干与五支线；再点击T5显示3单元、主资源/核查层次/先修/自查/选做/跳过条件。
- 手机390下scrollWidth375，无横溢出；主导航非活动色rgb(223,231,240)，活动色rgb(98,199,179)，正文实际字体为微软雅黑优先统一无衬线。
- 两名独立reviewer核查内容/渲染/保护。渲染探针55检查通过，但探针未作为仓库回归测试提交。

## 已完成部分
首页从方向页分离；三方向有对象/现状/日期/理由/限制/来源；20条论文含1deep、2standard、13quick、4entry；模板有结构差异而非只字数；六技术主干有19单元并各有一个可开始资源；五篇旧公开文献接支线且RAG综述去重；legacy与私人存储边界保留。此前链接色和按钮字体冲突已修。

## 待修项
1. P2 阅读目录与标题不一致：library.js:324,340 vs1478–1486。quick目录“为什么读这篇/读完后自查”与正文“为什么留意这篇/取全文时先核对”不同，entry也不同。复用同一章节描述生成目录和正文，补DOM逐项匹配测试。
2. P2 手机目录不可折叠：library.js:1369–1373/styles.css:764只把侧栏移到正文前，未实现DESIGN005要求的可折叠目录；deep长目录挤占开头。补移动折叠并真实验证。
3. P2 路线阶段提示自相矛盾：library.js:1166固定先后文案与routeStages首次出现分组顺序不一致；方向一评价在方法之前。保留批准论文顺序，按实际顺序生成提示，不为匹配提示重排论文。
4. P2 首页标题样式仍被覆盖：styles.css:445 .lib-view h3压过462 .lib-zone-title与473 .lib-firstuse-title；预期字号和零顶部外边距未生效。局部作用域修正并看计算样式。
5. P2 内容校验缺口：library.js:471–484未严格校验home.updatedOn/zone.entryLabel/howToUse/firstUse，遗漏可通过校验却渲染undefined。补缺字段变异用例和实际DOM断言。
6. 记录修正：ACCEPTANCE-005:33测试计数；PLAN004:56–66仍写未实施，与实际产物相冲突。保留历史但追加本次实施事实。

补充：deep卡有机制、证据和来源结构，但模板要求的明确辅助例子（或省略理由）未补。新增精读数字与原文逐项复核、隔离grad-radar保存及同源Markdown导出，本轮没有足够可重放证据签署通过；内容结构校验不能证明实际读过全部所标章节。

## 审查限制
截图生成工具仍仅返回路径而非可检视图像，本轮不宣称视觉通过。只验证了首页→技术概览→T5的真实点击，未完成桌面1280/1440与手机390下所有三档卡/目录/前后篇/legacy矩阵。未读取真实个人存储、private或.grad。无提交基线，旧文件“不变”只做源码接线与旧CSS哈希等间接核查，不宣称全文件字节比对。

## 回流
局部修订批次即可，不新建平台或后继方案。实施者修复上述条目并提供运行证据，再定点复验。此报告不授权自动修代码、迁移、删除、提交或部署。

## 复核（修复后，2026-09-15）
六项P2逐点实测关闭：
1. 摘要卡目录按钮与章节标题一致（compass卡missing为空）。
2. 手机390目录为details折叠（open=false，summary“本页目录”）；宽屏load后open=true且11条链接可见、summary隐藏。domcontentloaded瞬间open=false为渲染时序瞬时态，不影响稳定态。
3. 路线阶段提示与lib-stage-label分组顺序一致（建立问题→看评价与反例→理解方法→核查近期竞争），提示文案改为按实际顺序生成并声明保持论文原序。
4. .lib-zone-title与.lib-firstuse-title计算值17px、margin-top0。
5. validateLibrary补home.updatedOn/zone.entryLabel/howToUse/firstUse必填校验；tests/render.test.mjs（243行）把渲染探针固化为回归，覆盖目录一致、折叠随视口、首页字段与阶段顺序。
6. deep卡补“整理者的辅助例子（不在原文中，只演示机制）”。

实测npm test：201项总数、200通过、0失败、1条件跳过（Windows符号链接权限）。ACCEPTANCE-005:33已按环境差异如实记录并以验收方实测为准；其61/74行“0跳过/201通过”与验收环境计数不符，属实施方口径误差，不影响功能结论。

复核结论：上轮待修项全部关闭，主体功能验收通过。仍未闭合：全视图视觉矩阵与截图可检视证据、新增精读内容逐条原文复核与内容生产保存证据。
