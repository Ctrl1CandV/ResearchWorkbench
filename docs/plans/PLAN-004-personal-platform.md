# PLAN-004：个人研究首页、分级阅读与Agent学习体系

## 元数据
- 状态：设计已独立审查通过，交接基线冻结；未开始产品实施。
- 当前阶段：规划与设计。
- 当前执行点：审查配套文档，冻结后交后续模型实施；本会话不写应用代码。
- 来源：用户看完整站后的反馈，明确只要策划设计文档。
- 前序：PLAN-003-reading-library.md，保留其已实现功能、隐私与来源约束。
- 关联：../SPEC.md、../DESIGN-005-personal-research-platform.md、../READING-TEMPLATES-005.md、../TECH-LEARNING-005.md。
- 最近更新：2026-09-15。

## 原始计划

### 阶段0：保护基线与落实文档
目标：先明确当前能力与新范围，避免重建平台或误删资料。
范围：上述文档、现public与tests只读快照；由未来实施者创建临时基线，不提交私有记录。
步骤：读AGENTS/START-HERE/SPEC/本计划和配套文档；确认无并行写同文件；保存公开内容与旧CSS/路由基线；检查资源候选核验等级。当前仓库存在.workbuddy/未跟踪文件，未经检查和授权不清理、不纳入任务。
验证：列出保护字段和允许改字段；记录styles.css前321行（原旧版域边界，以实际文件复核）或当前旧样式边界hash（先按实际文件确认，不永久硬编码行数）。
风险回退：无法确定本人记录来源则不迁移；不读private/.grad做界面种子。

### 阶段1：内容模型和首批导读
目标：首页四区和方向理由有真实内容，模板分层与技术路线可落地。
范围：public/library-content.js，可按需新增公开content模块，但需白名单与测试同步；优先维持当前文件，不先拆平台。docs/research新公开导读及卡输出。
步骤：新增home编辑入口、方向overview/stateOfField/asOf/whyChoose/limits/sources；按DESIGN005三方向底稿回查公开来源。新增推荐深度和实际深度，单一权威字段与旧reading兼容适配；增有限block而非新模板引擎。落实六Agent技术主干、单元、主资源及可选旧论文支线。按READING模板完成至少1真实deep，另保standard/quick样本；来源不足如实记录，不先挂完成标签。建立公开身份映射，RAG综述去重。
验证：所有id/关联/来源链接可解析；每方向有完整说明；6技术主干有3单元以上且至少1可开始主资源；deep/standard/quick/entry依据门槛；三轴与深度独立；方向序与私人事实不漂移。新日期只用于本次真实核查。
风险回退：正文不可取得时保持原档位并报告阻塞；不能仅加长旧摘要。外链失败保留资料身份及替代入口，不编URL。

### 阶段2：首页与方向页面
目标：把方向页从“首页”中分离，让平台四区可理解且可进入。
范围：public/index.html、library.js、styles.css的新版域、tests/library.test.mjs。
步骤：新增#/home默认入口和第五导航“首页”，原#/directions仍为方向列表；学习详情路由当前已存在，只扩充其内容，不重复造。首页四区预览及使用说明，最新精选显示内容日期；编辑首读不是本人继续读。方向列表加短介绍，详情在路线前展示研究现状、理由、限制和来源。保留侧栏满视口和手机自动高度修复。
验证：首页四区实际点击进入真实内容；空hash与旧深链正确；无假阅读进度；三方向列表/详情文字一致；桌面/手机信息顺序稳定。
风险回退：新home失败可临时保留旧directions入口，但不得标此阶段完成；不改旧app的默认页面。

### 阶段3：分级卡与阅读排版
目标：深度与类型决定内容组织，正文层级清晰且字体一致。
范围：library.js内容渲染、styles.css新版域、library.test及样式回归测试。
步骤：实现3深度布局+entry态、类型章节配置；block白名单DOM渲染；统一正文/标题/操作/元数据字体职责。重点修复已定位的实际冲突：body.lib a覆盖侧栏链接和主按钮白字；.lib-btn未定字体；全局lib-view h2/h3和article规则竞争；Georgia优先导致中英数字混排。为侧栏与主按钮各状态写明确作用域，不能靠提升所有!important。
验证：用真实deep/standard/quick卡检验差异；显示来源不足降级；按钮/侧栏计算颜色对比合格，正文中英/数字/代码/表格层级一致；目录滚动不改hash；前后篇正常；所有外部href必须https白名单渲染，不执行raw HTML。
风险回退：复杂表/公式不满足来源时退回准确文字，不虚构图示。旧CSS全局零改动。

### 阶段4：技术学习与公开旧内容支线
目标：不选研究方向也能开始学习Agent必学能力。
范围：library-content.js、library.js、新版CSS和tests；旧content.js只读，app/domain/legacy保留。
步骤：技术概览区分主干/进阶/工具参考；详情含目标、先修、单元目录、资源、自查、选学实践、下一单元。relatedPaperIds可空。复用Toolformer/ToolLLM、Lewis RAG、LoRA/PEFT为支线，保留元数据/摘要覆盖，不能升级已读；RAG Survey同身份去重。用户仍能访问历史笔记；不导入画像匹配分或私人笔记。
验证：六主干均有实际起点；T5评价和T6安全可提前进入，不被线性完成锁住；资源权限/版本标明；支线与三研究方向分开；legacy同源路径仍200且原始storage未被新页面读写。
风险回退：支线内容未核不进入推荐；私人迁移和旧版删除另开用户授权任务，不以支线完成宣告全部旧版无用。

### 阶段5：独立审查与交付
目标：证明内容可用与排版可读，不只证明测试通过。
范围：新验收记录、README/START-HERE/SPEC状态、DESIGN.md当前实现记录；主协调或用户指定审查者。
步骤：唯一builder完成每批后交证据；独立reviewer不修改代码。一次批量覆盖桌面1280/1440、手机390：home/方向/三档卡/技术详情/精选/legacy入口；核对字体与颜色计算值、真实点击/目录/下一篇、截图视觉。最多一批集中修复再确认，重大内容问题不是“视觉微调”须明确阻塞。运行全套npm test，保留旧测试，新增内容和渲染行为断言，不以源码regex代替浏览器。
验证：DESIGN005七场景+模板门槛+技术最低量；无P1/P2未解释阻塞。工具若无法返回可见图像，记录未视觉验收，不把截图路径当通过。最终用户会要求主协调审查，不由builder自称全部验收完成。
风险回退：保留旧版与旧记录；无提交/推送/部署授权则不得执行。最终交付包含文件清单、内容覆盖、运行方式、测试原始结果及未测项。

## 实施记录
本轮仅新增规划文档并更新入口契约；未修改public/tests/server/package，未迁移或删除数据。公开旧内容和代码只读核查；成熟平台做限定来源核查，不冒充完整竞品审计。

## 独立审查
2026-09-15 命名reviewer完成四份设计与现有代码契约核对，初审提出技术主干起点门槛不一致。主协调统一为六主干各至少一个章节核查完成、具备访问条件和自查的可开始单元；同时明确deliveredDepth权威、现有学习路由和旧CSS边界。定点复审pass，无剩余设计阻塞。主协调接受修订并冻结此交接基线。资源补核和正文精读仍是后续实施任务，不能把设计pass当产品验收。实施后的审查由用户另行发起。

### REVIEW-001 · 2026-09-15 · 实施复盘（reviewer）
- 审查对象：实施结果（代码 + PLAN阶段0–5）。
- 产出复述：首页四区、三方向导读、20论文分级与六技术主干/旧公开支线已落地；不是仍未实施。
- 四维结果：主要功能正确性通过；完整性有目录折叠/深度内容证据缺口；回归测试189通过、0失败、1跳过；可维护性需补渲染DOM与内容缺字段校验。
- 已验证：npm test及浏览器首页→技术概览→T5真实点击；390宽度与字体/导航色计算值；独立双审源码/内容与渲染探针。
- 问题汇总：5项局部P2（目录标题、手机折叠、阶段顺序、首页h3优先级、home缺字段校验）；验收记录测试数量错误；deep辅助例子/来源生产证据待补。
- 决策路径：局部修订，无共享架构根因，不另建后继方案。
- 最终裁决：有条件通过主体功能，暂不认定全部完成；详情及关闭条件见 ../REVIEW-PLAN-004.md。截图不可检视，视觉与全交互矩阵未完成。
- 回流：实施者按报告修复，用户发起定点复核。本次未修改产品代码。

## 计划修订
无。后续进入执行中后冻结原始计划，新增变更用REV，不静默重写。

## 最终结果
规划已完成并独立审查通过，未实施。第一实施点：阶段0保护基线，随后阶段1内容模型与真实卡片；不能先改CSS后补内容。后续模型修改冻结范围须回流主协调裁决，不自行删旧版或放宽来源门槛。

---

## 实施记录追加（2026-09-15 晚，阶段0–4执行与审查回流）

以下为追加事实；上方原始计划正文与既有记录保持原样，不作回写。

- **阶段0–4已实施**：新增 `#/home` 首页四区与首次使用说明；方向页补研究对象/现状（含 asOf）/选择理由/限制/来源，路线按阶段分组（不改已批准论文顺序）；阅读卡按 `deliveredDepth` 分四种结构（1 deep / 2 standard / 13 quick / 4 entry），Astute RAG 精读卡依据本轮实际读取的 arXiv HTML 全文（2410.07176v2）产出；正文改有限 block 渲染（paragraph/list/comparison/callout/formula，白名单 DOM）；技术学习改为六条必学主干（19 单元，主资源已定向核查：Python 教程 Modules、pytest、Pro Git、MS 课程 01/11/14 课、LangGraph overview、OWASP GenAI、FastAPI）加五条按需支线，旧公开内容接入支线并去重。改动文件：public/library-content.js、library.js、index.html、styles.css（仅新版段，旧版 321 行哈希不变）、tests/library.test.mjs。
- **审查回流修复（对应 REVIEW-001 与 ../REVIEW-PLAN-004.md 的 6 项待修）**：项1 章节标题收敛为 `sectionTitle` 单一来源（PAPER_SECTION_TITLES），目录与正文不再漂移；项2 目录改 details 可折叠（≤900px 默认折叠、桌面默认展开，matchMedia 驱动）；项3 阶段提示按 routeStages 实际分组顺序生成，不硬编码；项4 首页 h3 标题加 `.lib-view` 作用域修正覆盖；项5 home 字段（updatedOn/zone.howToUse/entryLabel/firstUse 条目）校验补齐；项6 ACCEPTANCE-005 计数修正并追加本节。补充项：deep 卡补明确标注“不在原文中，只演示机制”的辅助例子；新增 tests/render.test.mjs 把渲染探针固化为仓库回归（41 视图、目录与正文 DOM 逐项一致、折叠开闭随视口、首页字段实际渲染、阶段提示实际顺序）。
- **修复后测试**：全套 `npm test` 200 项通过、0 失败、0 跳过（本机实测；验收环境曾测得 189 通过 + 1 符号链接权限跳过，差异已在 ACCEPTANCE-005 记录）。
- **仍未完成（如实记录）**：grad-radar 隔离登记与同源 Markdown 导出无可重放证据；桌面 1280/1440 与手机 390 全矩阵视觉与真实点击复验待验收方执行；未提交、未推送、未部署。
