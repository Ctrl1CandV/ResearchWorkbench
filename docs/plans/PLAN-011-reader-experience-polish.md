# PLAN-011 · 读者体验小幅优化（连贯讲解 / 读者语言 / 层级去重）

## 元数据

- 编号：PLAN-011（docs/plans 现存最大 010，取最大 +1）
- 状态：**实施完成（2026-09-24）——代码/内容层面独立复核 GO；视觉实页验收开放（未执行，不宣称通过）**
- 当前执行点：已全部完成——最终测试 **228 项 227 通过 0 失败 1 EPERM 条件跳过**、diff check 通过、contentVersion v5.4；无遗留修复进行中项
- 注意：server.mjs 工作区脏改动为 009 遗留（非 011，已核实）；本轮 notes/guidance/server 未动；改动未提交未推送。
- 关联：PLAN-010（执行中、阶段 G 走查项开放，本计划不阻断其复测）、docs/SPEC.md、docs/BACKLOG.md
- 内容输入（只读）：docs/research/guidance-display-source-audit.md、docs/research/ai-agent-landscape-source-audit.md 最终版；节点深化文字以两审计已核内容为限，不新增检索
- 创建日期：2026-09-24

## 背景与目标（主会话 2026-09-24 授权的三问题）

用户明确授权小幅优化，由 general-purpose 实施、主会话统筹独立审查，不重构、不扩大技术架构、不删来源与个人记录。

- **问题 2（验收报告腔）**：站点正文是给研究生看的学习读物，不是验收报告。内部契约词（PF-07、auditRef、contentVersion、「摘要+元数据级/框架级已核」）与内部编号（land-edge-*/mat-*/审计文档节号）反复出现在主阅读；声明类语句（「编辑排定，非论文引用」「非综述公认分类」）在四区、课题页、图谱页多处重复。
- **问题 4（讲解太浅）**：广域脉络图 20 节点目前多为术语释义级，只解释「是什么」，未建立大致知识体系；用户不必读原文，靠站内节点承担学习功能（PLAN-010 REV-001 目标未达）。
- **问题 5（层级不清）**：首页四区各带 purpose+howToUse 两段长文重复解释用途边界；主阅读入口重复（goal 区与 topic 区 entry 同指 `#/route/cross-harness-collab`）；课题页正文/比较表/辅助来源平铺无层级；旧内容全展开占屏。

目标：内容先行（关键节点连贯讲解＋读者语言正文）→ 展示轻调（层级与去重）→ 独立审查与真实验收。不搞全文精读冒名，不新增功能、库、模块或路由。

## 硬约束（违反即返工）

1. 保留全部旧论文/材料 ID、v3、既有内容与方向集合；`guidance.js`、`notes.js`、`server.mjs` 一字不动；009 `LIBRARY.map` 数据与校验不降级。
2. landscape 保持 20 节点 / 19 边 / 6 路径，**不增删节点/边/路径**；学术边来源 100% 不降级，**不冒引新来源**——来源从既有字段解析，找不到文献映射处保留可追溯的原始详情（如审计条目描述），不假装已映射；`contentVersion` 按实施时实际当前版本递增（当前 v5.3），旧论文 ID 相对顺序不变。
3. 不新增 public 模块文件、不新增路由、不动 server 白名单；确需新增一律回流 planner。
4. 来源说明**页面级只说一次**；具体证据限制（「只核到摘要」「表未逐格核」等）仍就近保留但压到一句以内；内部编号与内部契约词退出读者可见文本（保留在数据字段与测试断言中）。
5. 深化文字不得超出两审计已核范围；超出写「不展开」，不冒充精读；不搞机械字数配额。
6. 不提交、不推送；PLAN-010 遗留未通过的截图/真实交互走查项如实保持开放，本轮不得冒通过。

## 阶段 0 · 草案审查（冻结闸门）

- 本计划经独立只读设计审查 GO 后主会话冻结，方可下发实施。~~NO-GO 则修订后复审。~~
- 2026-09-24 设计复审 **GO**（主会话接受并裁决修订 4 项，已落入下文相应条目），状态转「执行中」。本 GO 仅覆盖设计面，产品代码未动。

## 阶段 A · 内容先行（数据与正文，不动渲染逻辑）

- A1 关键节点连贯讲解：`public/library-content.js` 的 `LIBRARY.landscape.nodes` 新增可选字段 `guide`（连贯讲解正文）。20 节点保留、不增删，选中 **10 个关键节点**（已逐一对照数据核实存在；审查方曾误把 land-b2 计入，**b2 不在选中列表**，仅作 land-b1 讲解内的承接）：`land-a5`、`land-a6`、`land-b1`、`land-c2`、`land-c3`、`land-d1`、`land-d2`、`land-d3`、`land-e1`、`land-e2`，覆盖主会话点名六组易混概念：
  - land-a5 / land-a6（**预训练 vs 对齐**：能力从哪来 vs 听不听话，对齐税与奖励黑客）
  - land-b1（**模型 vs Agent**：一次问答 vs 感知—规划—行动闭环；b2 的三处本质差别并入本节点讲解作承接）
  - land-c2 / land-c3（**推理 vs 规划**：单步想清楚 vs 多步分解重规划，误差沿链放大）
  - land-d1 / land-d2 / land-d3（**工具 / RAG / 记忆**：三只「向外的手」的分工与混淆点）
  - land-e1 / land-e2（**协作收益与成本**：分工收益 vs 通信成本与错误传播；**评测测试边界**：完成率看不见交互成本、测试通过≠正确）
  - guide 结构＝动机 → 机制小过程 → 具体例子 → 易混概念 → 与前后节点关系；每节点正文约 300–500 中文字为**写作参考、非测试配额**。
  - **讲解深度口径（主会话裁决修订 1）**：guide 必须真实解释机制/过程与贯穿例子，不得降回「术语三句」式释义；允许通识性编辑解释（超出摘要的常识串联），但须以「（编辑解释）」等方式**示例标注**、与有据内容区分；不要求每句逐字出自摘要，但**具体论文结论/数值/章节不得超出两审计已核依据**。不采纳「无米下锅一律不展开」的机械口径——现有基础概念可充分讲清。
  - 其余 10 节点（a1–a4、b2、c1、e3、e4、e5、f1）不做 guide，仅改善承接：在 limitation 或新增一句「与前后节点关系」衔接下一节点，消除术语孤立感。
- A2 读者语言改写（`public/content/*.js` 与 landscape 数据）：内部编号改读者语言（审计节号「§4 A1」等不进入正文，保留来源文献名）；「摘要+元数据级/框架级已核」改为一句读者可懂的证据说明；四区/课题页/图谱页重复的声明句收拢到各页一次（数据层先改文案，渲染层在 B3 配合）。来源展示从既有字段解析，不冒引新来源；找不到文献映射处保留可追溯详情，不假装已映射。
- 验证：guide 探针（10/10 非空、非「术语三句」回归——结构完整性抽查五要素齐全）、节点/边/路径数量与学术边来源 100% 不回退、**不设字数断言**、既有测试不回退。

## 阶段 B · 展示轻调（library.js / styles.css 轻改，无新功能）

- B1 首页去重与简写：**去重仅限「主推荐」语义角色**（主会话裁决修订 2）——「建议从这里开始」＋阅读目标区＝唯一主推荐入口；课题深化区入口改为「课题结构与论文库」语义、不再同指路线页首读；**导航/回链同 URL 不禁止**，次级行与页内正常链接保持可达；四区 purpose+howToUse 合并为一行「这区是什么 · 什么时候用」。
- B2 课题页层级：深化块按 正文导读 → 对照表 → 辅助来源（核查记录/未定候选） 三级排布，页内来源说明一次；旧内容（原对象/现状/理由/限制等）保留为可展开（复用既有折叠样式），默认收起、不占主阅读。
- B3 图谱页：节点详解 guide 优先展示、字段表退居其次；学术边显示来源文献名、编辑边显示「建议的阅读顺序（编辑安排）」，内部节号不显示；重复声明已在 A2 收拢，渲染不再逐条追加。
- 验证：渲染探针（主阅读入口唯一、四区单行说明、课题页三级顺序、guide 渲染、边显示无内部节号）；DG009 安全/地图数据契约测试不回退；被替代的展示断言逐条在实施记录中解释。

## 阶段 C · 独立审查与真实验收

- 独立只读代码审查（general-purpose 自检不算）；全量测试实跑不回退（010 基线 220 项）。
- 真实验收如实：390/1280/1440 截图与节点真实交互本轮仍按开放项登记，用户在可走查环境复测后由主会话复审；**原页面缺截图验收项保持「未通过」，不冒通过**。
- 按验收矩阵逐行记录前后差异；不提交不推送。

## 任务包（单一实现者，审查独立）

- 实施者：general-purpose 子代理（如实记录）；主会话统筹独立审查与验收。
- 写权限：public/content/*.js、public/library-content.js（contentVersion 按实施时实际当前版本递增）、public/library.js、public/styles.css、tests/。docs/SPEC.md、BACKLOG.md、本 PLAN、DESIGN.md 仅 planner。
- 不动：guidance.js、notes.js、server.mjs、旧 ID/v3、009 LIBRARY.map、论文事实。
- 顺序 A → B → C；每阶段完成追加 `## 实施记录`。

## 验收矩阵（前后差异）

| 维度 | 前（010 基线） | 后（011 目标） | 核验 |
|---|---|---|---|
| 关键节点讲解 | 术语释义级 | 10 关键节点连贯讲解（约 300–500 字为写作参考、非测试配额），其余承接改善 | guide 探针＋文本抽查 |
| 读者语言 | 内部编号/契约词/声明多区重复 | 页面级来源说明一次，证据限制就近一句，内部编号退出正文 | 读者文本内部标记扫描＋抽查 |
| 首页入口 | goal/topic 双入口同指路线页 | 主推荐入口唯一（导航/回链同 URL 不禁），四区说明单行化 | 渲染探针 |
| 课题页层级 | 块平铺、来源说明重复 | 导读→对照→辅助来源三级，旧内容可展开 | 探针＋390 抽查 |
| 图谱页 | 字段表平铺、边注带内部节号 | guide 优先、边注读者语言 | 渲染探针 |
| 测试 | 220 项 219 过 | 不回退＋新增绿 | 全量实跑 |
| 提交推送 | 未提交 | 仍不提交不推送 | git 不动 |

## 回退

git 未提交改动按文件级还原；数据按 contentVersion 递增、旧结构可读；不回退任何旧 ID 或 v3。

## 实施记录

- 2026-09-24：PLAN-011 草案创建（planner，依据主会话三问题授权）。
- 2026-09-24：**设计复审 GO（主会话接受）并裁决四项修订，状态转「执行中」、冻结**。裁决原文要点：①不设字数测试配额，但 guide 不得降回术语三句——须真实解释机制/过程与贯穿例子；通识编辑解释允许且须示例标注，不要求每句逐字出自摘要；具体论文结论/数值/章节不得超两审计依据；不采纳「无米下锅一律不展开」的机械口径，现有基础概念可充分讲清。②首页去重仅限主推荐语义角色，导航/回链同 URL 不禁。③不增节点/边/路径、不冒引新来源，来源从既有字段解析；找不到文献映射处保留可追溯详情，不假装已映射。④contentVersion 按实施时实际当前版本递增（当前 v5.3）。以上已全部落入正文相应条目。另核实选中 10 节点 ID 为 land-a5/a6/b1/c2/c3/d1/d2/d3/e1/e2（审查方误数 b2，b2 不在选中列表）。**记录：本 GO 仅设计面，产品代码未动、实施未开始。**
- 2026-09-24：阶段 A/B **第一轮实现完成**（general-purpose 实施者）：测试基线 **228 项 227 通过 0 失败 1 EPERM 条件跳过**（同一环境性符号链接项）；改动未提交未推送。
- 2026-09-24：第一轮**独立只读审查已执行**，发现问题：内部编号仍泄漏进读者可见文本、来源展示全折叠（不符合「页面级来源说明一次＋guide 后常显来源身份」口径）、contentVersion 未递增等。主会话已派修复 5 项：①内部编号换自然标题；②guide 后常显短来源身份与证据级；③去除 goal 区头部重复入口（不加新路由）；④contentVersion 升 v5.4；⑤内容纠正——BERT/GPT-3/DALL·E 并非同底座三种用法的表述修正、RAG 与记忆按本文自身维度区分（不照其他综述口径）、对齐非工具可靠性前提（是帮助而非前提）、复杂数值改为定性表述。**修复轮进行中；最终测试计数待实施报告到位后如实补记，此刻不写最终数。**另核实：`server.mjs` 工作区脏改动为 009 遗留（非 011 改动），011 实施未触碰。
- 2026-09-24 主会话裁决（记录留痕，不重写冻结计划正文）：阶段 C 验收口径明确——**代码/内容审查与真实实页验收分开记录**；390/1280/1440 截图与节点真实交互未走查通过前不得宣称视觉通过（不冒通过），如实保持开放。
- 2026-09-24：**修复轮完成＋最终独立复核 GO（网络恢复后执行，代码/内容层面）**。首轮 5 项修复落地；最终复核对末项作一处收窄——land-e2「过强」表述改为「有所减少但未消除」的定性口径，编号残留已清除；以上均经独立只读确认（guide 后 sourceBrief 常显来源身份与证据级 / contentVersion v5.4）。最终测试 **228 项 227 通过 0 失败 1 EPERM 条件跳过**（同一环境性符号链接项），diff check 通过。本轮 notes/guidance/server 未动；改动未提交未推送。**审查仅覆盖代码/内容层面：390/1280/1440 截图与节点真实交互本轮未执行、沿用开放项，不宣称总视觉验收通过。**

## 独立审查

- 2026-09-24 设计审查（独立只读）：判定 **GO**；主会话接受并裁决修订 4 项（见实施记录同日条目）。本 GO 仅覆盖设计面，产品实施未开始；代码级独立审查仍按阶段 C 执行。
- 2026-09-24 第一轮实现审查（独立只读）：发现编号泄漏/来源全折叠/版本未增等，主会话派修 5 项（见实施记录）。
- 2026-09-24 修复轮最终复核（独立只读，网络恢复后执行）：**GO（代码/内容层面）**——5 项修复确认落地（含 e2 定性收窄、编号残留清除、sourceBrief 常显、v5.4），最终 228 项 227 通过 0 失败 1 EPERM、diff check 通过。**范围声明：仅代码/内容层面；390/1280/1440 截图与节点真实交互本轮未执行、沿用 PLAN-010 开放项，不构成视觉通过。**

## 最终结果

- **实施完成（2026-09-24）**：阶段 A/B 内容先行与展示轻调全部落地，contentVersion v5.4；最终测试 228 项 227 通过 0 失败 1 EPERM 条件跳过（环境性），diff check 通过。
- **代码/内容审查：通过**（独立只读复核 GO，含首轮 5 项修复与 e2 定性收窄确认）。
- **视觉实页验收：开放**——390/1280/1440 截图与节点真实交互本轮未执行，沿用 PLAN-010 开放项；不宣称总视觉验收通过，待可走查环境复测后由主会话复审。
- 本轮 notes/guidance/server 未动；改动未提交未推送。
