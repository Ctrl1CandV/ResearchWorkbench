# PLAN-001：首版启动与交付

## 元数据

- 状态：已取代（用户否决实验主导定位；代码保留，不作为新版需求基线）
- 当前阶段：已停止
- 当前执行点：转入后继 PLAN-002-research-navigation.md；原始计划正文保持冻结
- 关联 SPEC：../SPEC.md
- ADR：暂无，技术选择未定
- 最近更新时间：2026-09-14

## 原始计划

### 阶段一：策划与约束核查

目标：冻结足够小且真实有用的首版范围。
范围：SPEC、定向调研结论与必要决策记录。
步骤：先验证三名命名智能体可调用；scout 只读核查本机 grad-companion 的记录契约与最小交接方式、相关公开文档；主协调者收敛首版场景和实际内容规模；reviewer 独立检查过度建设与关键缺口。
验证：每个首版能力对应验收条目；集成假设有文件/运行证据或明确限制；不能拿之前泛调研代替当前接口核查。
风险/回退：若技能无法直接调用，评估透明的文件/宿主交接，不先搭新代理后端；无法发现命名代理时保持阻塞，不伪装模型。

### 阶段二：设计与任务冻结

目标：给实现者可执行的交互、数据、错误和测试契约。
范围：必要设计文档、ADR与本计划草案。
步骤：主协调者确定应用形态、技术栈、持久化和插件边界；按需做小型可行性验证；独立审查后细化实现文件范围与测试命令；冻结计划再交 builder。
验证：关键行为无歧义、用户资料边界清晰、真实材料有出处、首版无需导入全部历史。
风险/回退：集成或资源不可得时缩小范围，更新 SPEC；不以演示占位掩盖缺失。

### 阶段三：小批量实现

目标：逐个完成可验收切片。
范围：由阶段二确定，当前不授权代理猜测应用结构。
步骤：builder 按明确任务单修改与补测试；主协调者整合；reviewer 检查稳定变更；问题回流修复。
验证：按冻结后的行为契约执行测试与真实运行；逐项记录完成、失败和未测。
风险/回退：单写者避免冲突，不碰 private 原始资料；仅回退团队创建的变更，不清理用户文件。

### 阶段四：独立审查和交付

目标：首版可实际使用且维护说明准确。
范围：验收证据、运行说明、已知限制和后续待办。
步骤：独立检验核心用户场景及数据恢复、插件边界、空/失败状态；关闭阻塞项；主协调者给出最终结论。
验证：实际运行而非仅静态代码检查；明确未验证的外部条件。未获授权不部署发布。
风险/回退：未通过保留工作成果并标注阻塞，不以降低验收标准宣告成功。

### 阶段三冻结任务包（2026-09-14 已冻结）

依据：DESIGN-001 v2、CONTENT-001 v1、SPEC 的显式收敛 AC-06（2026-09-14）。唯一代码写者 research-workbench-builder；协调者独占 docs 和 README 更新。范围不扩张。

切片 A：创建 package.json（type module，start=node server.mjs，test=node --test）、server.mjs、public/domain.js、public/content.js、tests/domain.test.mjs、tests/server.test.mjs、tests/content.test.mjs。领域模块导出纯验证/初始值/序列化及可注入 storage+locks 的仓库接口，所有写入必须同名锁下比较原文，不支持锁就停写；严格按 DESIGN 字段。固定资源白名单与 Host 检查；测试安全使用合成哨兵、不读真实 private。content 完整落地两任务与真实来源。运行 npm test；交回文件和输出、未测项。不得改 docs/README/AGENTS/.gitignore/private/.grad 或插件。

切片 B：仅 A 通过协调者检查后，创建 public/index.html、public/styles.css、public/app.js，按设计完成全部交互；补 tests 中实际需要的测试，不引入第三方后端/框架/远程资产。持久化失败和冲突保留草稿；损坏停止保存；导出区分原文与草稿；确认切换及重新载入。反馈读过/解释/复现独立。复制只公开目标；教程交接明确不是论文登记。npm test 后交独立审查。

协调者：实际浏览器使用隔离 origin http://127.0.0.1:4174 测试，交付 origin 固定 http://127.0.0.1:4173。包括桌面和390px、保存刷新、两路线恢复、未保存切换取消、原值损坏、存储读写异常、无锁/锁失败、双页竞争与草稿保留、复制失败；测试内容标记非本人进度。只读插件验证使用 tempfile 的空目录运行 list/show，记录退出码/stdout 和前后目录清单，不运行写入命令。不可把 CLI 验证称宿主阅读执行。最终 reviewer 检查代码及证据，问题由唯一 builder 修复。

风险/回退：浏览器故障注入能力不足则明确未测并保留阻塞；不降低数据保护标准。端口冲突换测试端口但不杀进程。没有真正的新生试用或学习环境运行则准确注明，不能宣称学习产物已由用户完成。

## 实施记录

- 建立 D:/Program Project/ResearchWorkbench 独立目录；复制历史导出到 private/legacy-export 并逐文件比对；未复制旧站代码、node_modules 或 .grad 数据。
- 核查三个 profile 的 model 字段与用户配置一致，scout 为 qwen3.8-max。
- 在原会话调用 research-workbench-scout 返回 type not found；当前工具仅发现 general-purpose、Explore 和两个文档 judge，未运行指定模型。新工作区会话加载为后续协作的前置条件。

## 独立审查

2026-09-14：reviewer 预审 changes-required，指出并发降级、静态白名单及 AC-06 证据歧义；协调者修订 v2 后最终设计审查 pass，批准先 A 后 B。此结论不是产品验收。

## 计划修订

草案期由协调者显式收敛 AC-06 为手动交接准备；补全 DESIGN-001 v2、CONTENT-001 v1 和任务包。独立审查后冻结；此后修改原始计划须 REV。

## 最终结果

未完成产品交付；当前仅工作区初始化与首版策划草案完成。
