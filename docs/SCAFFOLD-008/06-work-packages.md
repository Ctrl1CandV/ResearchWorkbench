# 06 工作包与验收

版本：008.2。每包一个实现者；同一文件不可多人并写。包按下面顺序执行，不再先接内容后补材料页，不以占位公开数据临时凑测试。此文档是实施顺序，不代表这些包已经完成。

## 公共任务简报（每次交接必须携带）

工作区：本仓库根目录（ResearchWorkbench，本地路径不写入公开文档）。
契约：SCAFFOLD-008 / 008.2，00–09；项目安全与协作约定仍以 AGENTS.md 为准。没有另获授权，不提交、不推送、不部署、不改个人记录、不动安装插件。
目的：两条起步方向、5+4 混媒介路线、三问与真实讲解、三条可学技术、精选感知。不是做共享状态平台，也不是重做 UI。
每包输出：所读契约、改动文件、完成项、实际检查与结果、来源/运行未核项、下一包前提。追加到已有的 IMPLEMENTATION-REPORT.md；不要修改 08 把文档审查冒充代码验收。

命名智能体只在宿主确实可发现且配置核对后使用；找不到就说明限制，不能用通用代理冒充指定模型。没有独立审查者时明确写“实施者自检，独立审查未执行”，不得声称 reviewer 已批准。

## 依赖

R 中断现场核对 → 沿用/补记 0 基线与 1 文档同步 → 补齐 2 兼容能力 → 3 起步内容与入口同时切换 → 4 技术栈与教材 → 5 精选及全局文案收尾 → 6 总验收。

包 3/4 的资料阅读可以先行，代码写入仍按顺序；都修改 library.js/聚合器/测试，默认不并行。包 2 只用测试 fixture 验证新模式，真实库保持旧内容可用。最终数量断言在各数据包落地后启用，不要求包 2 已经满足最终 featured=3。

## 包 R — 先核对中断现场，不能重新从零施工

前提：先读 09。当前文档已完成 008.2 同步，旧包 0/1 有历史记录，包 2 有未完成代码。
动作：重新查看工作区状态，核对 09 的现况是否仍成立；重跑测试，把当前数据和基线失败记入报告。复用已存在的解析/路由/校验，补齐真实渲染与测试。不删除断言掩盖失败，不将旧标签“下一篇”恢复来取悦过时测试。
退出：明确哪些代码可留、哪些需补、哪些旧选目引用要换；逐项列包 2 未完成项。若发现旧版新卡已落地，按 09 保留身份与记录，报告规模差额，不能静默删除。
写范围：此核对阶段仅报告；实际修复进入包 2。不要因包号较小而重写用户已完成的文档或清空所有实现。

## 包 0 — 只读基线（历史已做，R 补当前差异）

输入：AGENTS、SPEC、BACKLOG、SCAFFOLD README及00–09；按需读 READING-TEMPLATES-005、TECH-LEARNING-005、REWORK-007 与实际代码。
动作：记录当前 git 状态、原45论文 id及顺序、旧3方向谱系、旧11技术路线/单元、经典与简报，运行 npm test 保存结果。不要还原已脏文件、不要把未提交状态当失败。
写范围：仅实施报告；不改产品。
退出：知道哪些改动早于本任务，列明基线已有失败。历史127项是参考，不冒充本次实跑。
失败：保留输出并定位，能证明是原有缺陷且不阻塞本期的列入报告；不得删除测试掩盖。

## 包 1 — 同步目标与实施中状态（本轮文档已同步，续做只核差异）

前提：包0。读00与05。
写范围：SPEC、BACKLOG、DESIGN、TECH-LEARNING-005、READING-TEMPLATES-005、根README、实施报告；不改 public。
动作：记录本期用户授权，PF-01/06/11及quick与coverage澄清；技术旧六主干定义标为历史/保留内容，新默认以04为准。SPEC保留原当前产品状态，另记“008实施中”；暂缓深读卡生产。
退出：旧文档不再把“两方向”“三技术”误判为无授权重排；不宣称产品已完成。文档相对链接可解析。
回退：只撤销自己写入的目标补丁，不覆盖历史或用户原改动。

## 包 2 — 兼容渲染、路由与校验先行（已部分开始，须补齐）

前提：包1。读03、04的数据扩展、05的A01–A13/A15。
写范围：library.js、三个测试文件、实施报告。本包不改真实内容数据，不要求空 materials.js 入库。先按 09 补全 validateLibrary/renderApp/首页/侧栏/导航的实际消费，再核现有辅助函数，不用已有函数名证明功能完成。
动作：
- 兼容旧 route/startHerePaperId/无materials；新增 NodeTarget、材料页、安全讲解、query解析、track导航、typed首页、active筛选。
- 新方向与 featured/lesson/labPath 数据只在测试 fixture 中验证，旧真库仍走回退。
- 新旧 quick 标注共同函数、全局文案接口；默认旧 technicalRoutes 没 featured 时继续旧列表，有 featured 后才切三条+details。
- 添加 tech-t4 显式别名的解析逻辑；材料 no-record 和带query论文记录隔离。
退出：新增fixture正反例与原测试通过；旧首页与旧书签仍能用，材料/论文混合fixture整条能走通。此时不宣称新课程已发布。
回退：撤销本包自身改动即可恢复旧数据行为；不使用破坏性 git 命令。

## 包 3 — 两条起步路线、真实内容与首页原子切换

前提：包2。读01、02、03与07。不得在材料页尚不可用时挂新首页。
写范围：directions.js、materials.js（新）、papers-collab.js（新）、papers-routes.js的三张旧卡、library-content.js、server.mjs、三个测试文件、实施报告；必要的渲染缺陷只由同一实现者修在 library.js。
动作：
- 定向读原始资料，交四新卡（Beyond Frameworks/MemGPT/Tax/Debt）、600–900字问题导读和400–700字实证研究读法两篇primer；三张旧卡补三问/动作。Tax默认quick，standard须全模板。Debt结果定位不能只靠摘要。
- 一次性激活两方向及全部5+4节点；旧方向变deferred，三条旧谱系保留。主archive三项仅external，核不到身份则pending，不为按需目录造新卡。
- 同包切 typed startHere、firstUse、首页/侧栏方向计数与元信息。startRoute/archiveRoute权威，新真数据无旧route或startHerePaperId。
- 服务器精确新增 /content/materials.js、/content/papers-collab.js，聚合器接入；49论文/2材料/4方向断言此包启用。
退出：5+4节点全部ready并可前后走通；三问、Explain、Skip和来源抽检通过；TOSEM start下一步为Agentless，archive仍为Le；SWE-bench start结束。未核材料保留pending可诚实展示，但本包不能记完成。
失败：先用同作品官方版本，站内问题导读按已核来源编写，不强制用另一博客替换；仍缺来源则记录阻塞，不要求用户替AI审选目，不造假内容。其他互不依赖内容可继续制作。
回退：仅回退本包新入口与本包数据接入，包2兼容支持保留；旧卡原正文、覆盖与用户记录不动。

## 包 4 — 三条技术栈与贯通教材

前提：包3；资料核查可提前。读04和07技术资源。
写范围：technical-routes.js、multiagent-lab.md（新）、library.js、library-content.js的技术入口文案、server.mjs、三个测试文件、实施报告；运行环境只用独立临时练习目录。
动作：MA四单元共用Python+LangGraph示例；三单元RAG保留原目标且补完整starter；浅图三个单元含真实G3短文。T4规范id迁移与旧别名，其他十条折叠、清除过时方向编号/主线定位文字。服务器只再增一个精确教材路径。
验证：实施者照教材运行mock七类检查，锁定测试版本、记录SQLite跨进程恢复及取消/重试出口；实际API未测单列。打开MA各单元的教材链接和G3全文。旧T4链接含unit仍可用。
退出：真实库13技术/26单元/3featured；四MA教材可读可复制且mock已验；各主资源定位、权限、版本说明真实，不以资源名拼出“完整栈”。教材不接入工作台运行时。
失败：若隔离环境不能下载依赖，保留教材与失败证据但标“练习运行未验收”，不能只以npm test通过代替；不擅自改成另一框架绕过。
回退：可暂不启用featured入口，保留原单元；只撤销本包改动。不得删除用户旧练习或个人数据。

## 包 5 — 全局与每日精选文案收尾

前提：包4。读05 §3/4。
写范围：library-content.js、library.js、render测试、实施报告；仅必要局部排版修复可触碰styles.css并解释。
动作：排查“三个方向/六条必学/十一条顺序/quick一概仅摘要/所有表格未核”之类旧绝对文案，按实际状态更正。首页与brief感知口径一致；保留漏斗日期/失败边界，不改历史简报。
退出：默认入口统一、没有第二套课表或误导的下一篇；精选不进阅读任务。原API/存储不变。

## 包 6 — 总验收与交付

前提：包3–5实质完成。读05验收矩阵、08门禁。
写范围：必要测试、实施报告、SPEC/BACKLOG/README的真实完成状态；发现缺陷退回对应包修复，不能靠放宽本包标准。
必须执行：
1. npm test，报告总数/通过/失败/条件跳过及其原因；数据规模、旧数据保留、服务器白名单安全检查。
2. 逐项来源与正文抽检：5+4，Tax/Debt口径，TOSEM实际讲解、quick inline、站内问题导读编辑例子；主技术资料与教材七项运行证据。
3. npm start 在本机端口启动后，真实浏览器从首页走主线5步、代码线4步，刷新/后退/复制深链、start/archive切换、旧方向与T4书签、Explain定位、材料无记录、教材GET/下载。不要访问真实用户localStorage做破坏性测试；记录保存/导出用隔离浏览器上下文。
4. 本期新内容窄屏溢出冒烟；全站历史视觉矩阵仍保留BACKLOG，不能顺手打勾。
5. 检查改动范围和未授权写入；不提交、不推送。

报告逐项引用A01–A15结果，以及人工内容、教材、浏览器证据。未执行项、pending节点、外部阻塞单列；任何起步节点不可用、Explain虚标、教材mock未验、主导航错误或存储回归，都不能写“本里程碑完成”。真实API未运行可作为已知边界，不夸大验证范围。

## 合同变更规则

字段实现细节可在上述边界内选择；选目、路线数、技术主框架、存储或平台功能边界不得临场扩展。若实际源码与契约存在无法兼容的重大冲突，先在报告指出证据和最小修订建议，再请求裁决；一般URL迁移、局部兼容修复和编辑措辞不退回让新生审论文。

本期不含：共享状态服务/MCP/多Agent后台、GPT12周课表、个性化画像、旧卡批量升深、全网check-links、定时发现、git提交/推送。教程里的隔离教学代码不构成本平台实现研究原型的授权。
