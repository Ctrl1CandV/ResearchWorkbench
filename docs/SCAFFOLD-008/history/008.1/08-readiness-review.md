# 08 实施就绪复核

版本：008.1；日期：2026-09-21。结论：本版已补齐前轮六项文档缺口，可以按06交给实施者推进。这里的通过是“范围、字段、依赖和验收已能执行”，不是产品代码或教学示例已经完成，也不是保证后续不会发现问题。

## 1. 六项缺口的闭合

| 原问题 | 已确定的修订 | 实施时如何验收 |
|---|---|---|
| 技术主线是概念清单，不是完整可学栈 | 04固定Python+LangGraph四个连续单元、同一个算术任务、唯一主教程、完整下载教材、mock与可选real模式 | 包4实跑七类mock检查；版本、恢复、重试与取消出口有记录；不能只交资源名 |
| Explain有标签而无实际整理 | 03明确非空inline blocks或有效sectionId二选一；缺讲解不显示 | A03/A09；TOSEM现有design正文引用、quick短讲解、博客编辑例子实际可读 |
| 新短路线被旧next/旧首页/侧栏带偏 | typed startHere；hash保留route/track；新track为权威，旧next仅延伸；active统一过滤 | A04–A08；TOSEM start→Agentless而archive→Le，SWE-bench start结束；刷新后不串路线 |
| Tax/Debt证据范围过宽 | 07区分摘要身份与正文支持；Debt分别报告事件数、累计token、初始长度；Tax同脚手架设置不外推商业harness | 包3/6逐项核原文；未核正文不能写比较排名；不预设结构化或raw胜出 |
| 材料记录落点未决且现保存器会丢items | 明确本期无材料记录；notes.js/v3键、结构、导出不改 | A10；浏览材料零写入，论文仍用原id，query不污染记录 |
| 工作包依赖倒置与过早固定规模 | 包2先兼容能力，包3一次接起步内容及首页，包4技术教材，包6总验收；最终数量与通用validator分开 | 每包退出条件；包2用fixture不发占位内容；新入口只在页面可用后启用 |

同时闭合：主archive十项只用外链目录；新四论文/三材料范围固定；T4规范id及别名唯一；G3有实际正文且窄范围支持lesson；MA主资源不会因复用标记产生两个primary；教材是精确白名单纯文本下载，不在浏览器或工作台执行。

## 2. 本轮做过的检查

- 读取现有聚合器、路由/校验/渲染、技术数据、原卡、服务静态白名单和项目契约，核对新约定能映射现有结构。
- 实际导入当前LIBRARY：45论文、3方向（9/8/11步）、11技术路线/19单元、11经典。目标49/4/13/26/11来自明确增量，不是让实施者临时估数。
- 定向核对公开原始资料，定位Tax实验设置、Debt正文指标，以及LangGraph各主教程、LlamaIndex starter、NetworkX和Distill；具体读取边界见07。没有运行教学代码或调用付费模型。
- 实跑现有npm test：127项，126通过、0失败、1条件跳过（符号链接权限条件）。这只是未实施产品的基线，不证明008新增功能通过。
- 对00–07的字段、数量、工作包先后和本地文档链接做交叉复核。当前仅修改本目录及SPEC/BACKLOG的入口说明；没有实施产品、改个人记录、提交或推送。

本轮为主会话自检，未声称有独立命名reviewer审查。后续若独立审查工具可用按AGENTS核对角色；不可用应如实报告，不伪造验收人。

## 3. 后续门禁：什么完成了才能交付

1. 两条路线6+4所有节点ready，三问、实际讲解与覆盖一致；方向仅两条默认可见，旧内容仍可查。
2. 首页/混媒介/两种track/旧书签导航正确；材料不写记录，论文旧记录和导出无回归。
3. 三技术路线可学；主教材完整且mock七类检查实跑，旧技术内容保留。真实API未执行可作为明确边界，不能假称已验证。
4. A01–A15自动检查、内容人工核查、真实浏览器点击分别有证据；安全白名单、CSP、私有资料和不自动已读等纪律未放宽。
5. IMPLEMENTATION-REPORT.md说明每包结果、命令、来源、运行环境、待核和未执行项；SPEC只按真实完成情况更新。仍有起步pending、虚标讲解、关键导航/存储错误或mock未验时不标完成。

来源临时不可访问、依赖下载受限、浏览器工具缺失等是执行期可能的阻塞，不需要现在让新生再审选目；实施者先做文档规定的替代与检查，再具体报告未完成项。不能用“文档已就绪”免除执行验证。

## 4. 未扩大用户的任务

本修订不回到P0–P5架构清单、不做12周大课表、不引入画像/项目记忆数据库。Python+LangGraph是用于满足“完整可学技术栈”的教学选择，不是预选科研通信方案。研究候选通道仍包含原始轨迹、文档/wiki、结构化状态、交接包与检索记忆；谁更好仍待实验。目的三后置、每日精选只感知、AI不代替本人积累均保持。
